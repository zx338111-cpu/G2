# G2 底盘故障排查流程

> 适用场景：调用 GDK Python API 控制底盘，机器人不动或报错。

---

## 排查总流程图

```
底盘不动
    │
    ├─ 第一步：确认硬件是否正常
    │       ↓
    │   电机上电？急停激活？→ 查电源状态
    │       ↓
    │   硬件正常 ──────────────────────────────────────┐
    │   硬件故障 → 现场检查 CAN 总线 / 急停电路         │
    │                                                   │
    ├─ 第二步：确认指令有没有发出去                      │
    │       ↓                                           │
    │   查 quark_navigation.INFO 日志                   │
    │   看有没有 "subscriber connect" 我们的 PID        │
    │       ↓                                           │
    │   指令没到 → 检查 DDS 网络 / GDK 初始化           │
    │   指令到了 ────────────────────────────────────── ┤
    │                                                   │
    ├─ 第三步：检查任务系统                              │
    │       ↓                                           │
    │   pnc.get_task_state()                            │
    │   state=2？ → 有任务在占控制权                    │
    │       ↓                                           │
    │   pnc.cancel_task(ts.id) → 取消后再试             │
    │       ↓                                           │
    │   用 move_chassis 还是不动 ─────────────────────┐ │
    │   用 relative_move 动了 → 问题解决 ✓             │ │
    │                                                  │ │
    └─ 第四步：确认里程计来源 ◄────────────────────────┘ │
            ↓                                           │
        SLAM 可用？→ slam.get_odom_info()               │
        不可用 → 查 dr_state_machine 日志               │
        DR 在跑 → relative_move 可用，move_chassis 不可靠│
        DR 也挂了 → 重启 quark_navigation 进程          │
                                                        │
最终结论 ◄──────────────────────────────────────────────┘
优先使用 relative_move，放弃 move_chassis
```

---

## 第一步：确认硬件状态

**执行：**
```python
import agibot_gdk, time
agibot_gdk.gdk_init()
robot = agibot_gdk.Robot()
time.sleep(1)
ps = robot.get_chassis_power_state()
```

**检查项：**

| 字段 | 期望值 | 异常处理 |
|------|--------|----------|
| `chassis_left_traction_motor_power_state` | 1（上电） | 检查电源板和保险丝 |
| `chassis_right_traction_motor_power_state` | 1（上电） | 检查电源板和保险丝 |
| `emergency_stop_pedal_state` | 0（未激活） | 松开急停踏板 |
| `emergency_stop_pedal_fault_state` | 0（正常） | 检查急停电路，可能是瞬态，重启后自动清除 |
| `chassis_power_board_state` | 1（全功率） | 检查底盘电源板 |

**查看整车故障：**
```python
ws = robot.get_whole_body_status()
print("底盘错误码:", ws["chassis_error"])  # 0 = 正常
```

**判断：**
- 全部正常 → 进入第二步
- 电机未上电 / 急停激活 → 需要现场物理处理

---

## 第二步：确认指令是否到达导航系统

**方法：** 访问机器人上的 HTTP 日志服务器

```bash
# 从机器人本地访问
curl http://localhost:8849/latest/quark_navigation.INFO | grep "python3.10"
```

**期望看到（说明 DDS 连接成功）：**
```
subscriber connect to publisher success
topic /pnc/remote_control_cmd
peer d2379|python3.10|p<你的PID>
```

**没看到** → DDS 通信问题，检查：
```bash
# 确认 aorta 服务在跑
ps aux | grep aorta-service

# 确认 GDK 服务在跑
ps aux | grep gdk_service

# 确认环境变量已 source
echo $PYTHONPATH   # 应包含 /home/agi/app/gdk/lib
```

---

## 第三步：检查任务系统状态

这是最常见的不动原因。

**执行：**
```python
pnc = agibot_gdk.Pnc()
time.sleep(0.5)
ts = pnc.get_task_state()
print(f"id={ts.id}  state={ts.state}  type={ts.type}")
```

**state 含义：**

| state | 含义 | 处理方式 |
|-------|------|----------|
| 0 | 空闲，可以直接发指令 | 正常 |
| 2 | 有任务正在运行，底盘被占用 | 先 cancel_task |
| 3 | 上一个任务成功完成 | 正常，可发下一条 |
| 7 | 任务已取消 | 正常，可发下一条 |
| 9 | 任务完成 | 正常，可发下一条 |

**发现 state=2 时：**
```python
pnc.cancel_task(ts.id)
time.sleep(0.5)
ts = pnc.get_task_state()
print(f"取消后 state={ts.state}")  # 期望 7
```

**取消后再发 move_chassis 还是不动：**
→ move_chassis 本身不可靠（依赖控制权抢占），换用 relative_move

---

## 第四步：选择正确的控制方式

**先用 relative_move 测试：**
```python
req = agibot_gdk.NaviReq()
req.target.position.y = 0.3    # 向左 0.3m
req.target.orientation.w = 1.0
pnc.relative_move(req)

# 观察任务状态
for _ in range(20):
    time.sleep(0.5)
    ts = pnc.get_task_state()
    print(f"state={ts.state}")
    if ts.state in (3, 7, 9):
        break
```

**结果判断：**

| 现象 | 原因 | 解决 |
|------|------|------|
| state 从 2 变为 9，机器人动了 | 正常 ✓ | 使用 relative_move |
| state 一直是 2，机器人没动 | DR 里程计未工作 | 见第五步 |
| 立刻报 RuntimeError | 任务未完全清除 | cancel_task 后等待 0.5s 再试 |

---

## 第五步：验证死算里程计（DR）是否工作

relative_move 依赖 DR（Dead Reckoning，IMU 推算），SLAM 不可用不影响它。

**验证 DR 是否在运行：**
```bash
# 查看 DR 日志（从日志服务器）
curl http://localhost:8849/latest/quark_navigation.INFO | grep "dr_state_machine"

# 或直接查 dr 日志文件
curl "http://localhost:8849/dr_state_machine.log.INFO.$(date +%Y%m%d)*" | tail -20
```

**DR 正常的日志特征：**
```
dr.cpp:265] Precessing Frame ID: 309
dr.cpp:293] Robot Is Static        ← 静止时显示
dr.cpp:310] ZUPT: ...              ← IMU 零速修正在工作
Odom Publish Callback              ← 里程计数据在发布
```

**DR 不工作的处理：**
```bash
# 查看导航系统进程是否在跑
ps aux | grep quark_navigation

# 如果没有，重启导航
/home/agi/app/bin/navigation/bin/quark_start_g02.sh
```

---

## 第六步：查看故障管理器（有硬件报错时）

```bash
curl http://localhost:8849/latest/fault_manager.INFO | grep "fault_code" -A 4
```

**常见故障及处理：**

| 故障描述 | 原因 | 处理 |
|----------|------|------|
| `断联 Communication` 4 个车轮关节 | CAN 总线通信中断 | 检查底盘 CAN 线缆，重启底盘 |
| `emergency_stop_pedal_fault_state=1` | 急停踏板电路故障 | 通常是系统启动瞬态，重启后自动消失 |
| 关节 `Over-temperature` | 电机过热 | 停机散热，检查负载 |
| 关节 `Position limit exceeded` | 超出关节限位 | 手动复位关节到安全范围 |

---

## 快速诊断命令速查

```bash
# SSH 登录机器人
ssh agi@10.20.15.169   # 密码: <robot-password>

# 加载环境
source /home/agi/app/env.sh

# 查看关键进程
ps aux | grep -E "quark_navigation|genie_motion|gdk_service|aorta"

# 访问日志服务器（浏览器或 curl）
http://10.20.15.169:8849/

# 常用日志
http://10.20.15.169:8849/latest/fault_manager.INFO
http://10.20.15.169:8849/latest/quark_navigation.INFO
http://10.20.15.169:8849/latest/teleop_main_node.INFO
http://10.20.15.169:8849/latest/hal.INFO
```

```python
# 一键状态检查（Python）
import agibot_gdk, time
agibot_gdk.gdk_init()
robot = agibot_gdk.Robot()
pnc   = agibot_gdk.Pnc()
time.sleep(1)

ps = robot.get_chassis_power_state()
ws = robot.get_whole_body_status()
ts = pnc.get_task_state()
mc = robot.get_motion_control_status()

print(f"电机上电:   左前={ps.chassis_left_traction_motor_power_state} 右前={ps.chassis_right_traction_motor_power_state}")
print(f"急停状态:   踏板={ps.emergency_stop_pedal_state} 故障={ps.emergency_stop_pedal_fault_state}")
print(f"底盘错误码: {ws['chassis_error']}")
print(f"运动模式:   {mc.mode}  (5=G2伺服 正常)")
print(f"当前任务:   id={ts.id} state={ts.state} type={ts.type}")
agibot_gdk.gdk_release()
```

---

## 结论：底盘控制最佳实践

```
1. 优先用 relative_move，不用 move_chassis
   relative_move → 任务系统 → DR 闭环 → 稳定
   move_chassis  → 需抢控制权 → 容易被阻塞 → 不稳定

2. 每次发指令前先取消旧任务
   ts = pnc.get_task_state()
   if ts.state not in (0, 7, 9):
       pnc.cancel_task(ts.id)

3. 连续动作之间留 0.5s 缓冲
   任务 state=9 后，导航系统还需时间重置，
   不等待会报 "Task is not in IDLE" 错误

4. SLAM 不可用不影响底盘运动
   relative_move 用的是 DR（IMU），不依赖激光雷达

5. 调试优先看日志服务器
   http://10.20.15.169:8849/latest/fault_manager.INFO
```
