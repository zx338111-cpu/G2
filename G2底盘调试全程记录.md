# G2 机器人底盘调试全程记录

**日期：** 2026-06-02  
**机器人：** Agibot G2 人形机器人  
**SSH：** `agi@10.20.15.169`（密码：`<robot-password>`）  
**目标：** 通过 GDK Python API 实现底盘精确运动控制

---

## 一、G2 系统架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│                        外部开发者                                │
│                  SSH / Python GDK API                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    GDK 层（用户接口）                             │
│                                                                   │
│  gdk_service（守护进程）     gdk_http_server（:8849 日志服务器）  │
│                                                                   │
│  Python API：                                                     │
│    agibot_gdk.Robot()   → 关节状态、手臂、头部、电源状态           │
│    agibot_gdk.Pnc()     → 底盘运动、任务管理                      │
│    agibot_gdk.Slam()    → SLAM 建图、定位                         │
│    agibot_gdk.Camera()  → 相机图像                                │
│    agibot_gdk.Lidar()   → 激光雷达                                │
│    agibot_gdk.Imu()     → IMU 数据                                │
└────────────────────────┬────────────────────────────────────────┘
                         │ DDS 话题（aorta 中间件）
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    中间件层（aorta DDS）                          │
│                                                                   │
│  类似 ROS2 的 DDS 通信框架，基于共享内存（SHM）实现进程间通信     │
│  服务发现：etcd（http://10.42.1.101:2379）                        │
│                                                                   │
│  关键话题：                                                       │
│    /pnc/remote_control_cmd          ← move_chassis 速度指令       │
│    /pnc/task_service/relative_move  ← relative_move 位移指令      │
│    /pnc/task_state                  ← 任务状态反馈                │
│    /slam/state                      ← SLAM 状态                   │
│    /MotionControlService/JointPosition ← 关节位置控制             │
└────────┬───────────────────────────────────┬────────────────────┘
         │                                   │
         ▼                                   ▼
┌────────────────────┐             ┌─────────────────────────────┐
│  quark_navigation  │             │    genie_motion_control      │
│  （导航进程）       │             │    （运动控制进程）           │
│                    │             │                               │
│  负责：            │             │  负责：                       │
│  · 底盘运动规划     │             │  · 全身关节伺服控制           │
│  · 相对位移执行     │             │  · 手臂/腰部/头部 22 个关节   │
│  · 任务调度管理     │             │  · WBC（全身控制）            │
│  · 速度指令下发     │             │  · 末端执行器                 │
│                    │             │                               │
│  内部子系统：       │             │  控制模式：                   │
│  · dr_state_machine│             │  0=停止 1=G1伺服              │
│    （死算里程计）   │             │  2=路径规划 5=G2伺服          │
│  · slam_state_machine            └─────────────────────────────┘
│    （SLAM 定位）    │
│  · freespace       │
│  · tagloc          │
└────────┬───────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      HAL 层（硬件抽象层）                         │
│                                                                   │
│  hal（上肢：手臂/腰部/头部）   hal_lowerlimb（底盘）              │
│                                                                   │
│  通信协议：EtherCAT / CAN Bus                                    │
└────────┬────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────────┐
│                        硬件层                                     │
│                                                                   │
│  上身（22关节）：                                                  │
│    腰部 5 个、头部 3 个、左臂 7 个、右臂 7 个                     │
│                                                                   │
│  底盘（4轮全向）：                                                 │
│    idx111_chassis_lwheel_front_joint1                             │
│    idx121_chassis_lwheel_rear_joint1                              │
│    idx131_chassis_rwheel_front_joint1                             │
│    idx141_chassis_rwheel_rear_joint1                              │
│    驱动模式：蟹行（全向平移）/ 阿克曼（差速转向）                  │
│                                                                   │
│  传感器：                                                         │
│    激光雷达 ×2（前/后）、IMU ×3、相机 ×11、超声波雷达             │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、两种底盘控制方式对比

| 方式 | API | 路径 | 定位来源 | 特点 |
|------|-----|------|----------|------|
| **速度开环** | `pnc.move_chassis(Twist)` | GDK → `/pnc/remote_control_cmd` → quark_navigation → 底盘 | 无 | 需抢控制权，持续发送，不保证精度 |
| **位移闭环** | `pnc.relative_move(NaviReq)` | GDK → `/pnc/task_service/relative_move` → quark_navigation → DR闭环 → 底盘 | 死算里程计（IMU） | 任务式，自动闭环，到位停止，推荐 |

---

## 三、调试全程记录

### 3.1 第一次尝试：move_chassis（失败）

**操作：**
```python
pnc.request_chassis_control(1)   # 申请蟹行控制权
pnc.move_chassis(twist)          # 发速度指令
```

**现象：** 脚本执行完毕，机器人纹丝不动。

**初步猜测：**
- 速度参数方向错？
- 急停踏板故障（`emergency_stop_pedal_fault_state=1`）阻塞？
- 底盘电机没有上电？

---

### 3.2 状态排查

**检查底盘电源状态：**
```
左/右牵引电机电源：1（上电）
左/右转向电机电源：1（上电）
急停踏板状态：0（未激活）
急停踏板故障：1（故障！）
底盘电源板：1（全功率）
电池SOC：32.8%
```

**初步判断：** 电机已上电，急停未触发，但故障标志 = 1，疑似阻塞运动。

---

### 3.3 深入排查：任务系统

**检查运动控制状态：**
```python
mcs = robot.get_motion_control_status()
# 运动模式: 5（G2伺服）✓
# 错误码: 0 ✓
```

**发现关键线索：**
```python
ts = pnc.get_task_state()
# id=2, state=2, type=3   ← 有一个正在运行的任务！
```

> **根本原因（第一层）：** 系统中有一个 type=3（导航任务）正在占用底盘控制权，`move_chassis` 的速度指令被压制，无法下发到底盘执行器。

---

### 3.4 尝试 relative_move（首次）

取消阻塞任务后尝试 relative_move：

```python
pnc.cancel_task(ts.id)   # state: 2 → 7（已取消）

req = agibot_gdk.NaviReq()
req.target.position.y = 1.0   # 向左 1m
pnc.relative_move(req)
```

**任务状态变化：**
```
state=2（运行中）× 14次（约7秒）
state=9（完成）
```

**现象：机器人向左移动了约 1 米！** ✓

---

### 3.5 排查 move_chassis 不可用的深层原因

继续查看 `fault_manager.INFO` 日志（通过 GDK HTTP 服务器 `:8849` 访问）：

```
E fault_manager: Publish fault list, fault size: 4
  idx111_chassis_lwheel_front_joint1 → 断联（Communication）
  idx121_chassis_lwheel_rear_joint1  → 断联（Communication）
  idx131_chassis_rwheel_front_joint1 → 断联（Communication）
  idx141_chassis_rwheel_rear_joint1  → 断联（Communication）
```

> **注意：** 这是 2026-03-14 系统启动初期的瞬态故障，随后自动清除（日志频率从 10 降至 0）。`move_chassis` 不工作的真正原因是**阻塞任务**，而非硬件故障。

---

### 3.6 为什么 relative_move 能工作，move_chassis 不行？

**排查 SLAM 状态：**
```python
slam.get_odom_info()   # 失败：GetOdomInfo failed
slam.get_curr_pose()   # 失败：GetCurrPose failed
# slam_state = 0（未激活）
```

SLAM 不可用，但 `relative_move` 仍然成功——说明它使用的是**不同的里程计来源**。

**查看 dr_state_machine 日志（今日实时）：**
```
I0602 16:41:43 dr.cpp:293] Robot Is Static
I0602 16:41:43 dr.cpp:310] ZUPT: -0.000153536 ...
I0602 16:41:44 dr.cpp:265] Precessing Frame ID: 309
```

> **结论：** `relative_move` 使用的是 **quark_navigation 内部的死算里程计（Dead Reckoning）**，基于 IMU 数据推算位移，完全不依赖 SLAM 激光雷达定位。这就是它在 SLAM 不可用时依然正常工作的原因。

**两种方式的本质区别：**

```
move_chassis（速度开环）
    需要 GDK 抢到底盘控制权
    → 受其他任务阻塞
    → 不稳定

relative_move（位移闭环）
    直接提交给 quark_navigation 任务队列
    → 导航系统接管，用 DR 闭环执行
    → 稳定可靠
```

---

### 3.7 连续动作的 Bug 修复

**问题：** 第一个动作完成（state=9），立刻发第二个动作报错：

```
RuntimeError: RelativeMove failed
reason: Task is not in IDLE, FAIL, SUCCESS or CANCELED state
```

**原因：** state=9 只是 GDK 层的任务完成标志，导航系统内部还有约 0.5 秒的状态重置过程，此时发新指令会冲突。

**修复：** 在检测到完成状态后增加缓冲等待：
```python
if ts.state in (3, 7, 9):
    time.sleep(0.5)   # 等导航系统内部重置
    return ts.state
```

---

## 四、最终解决方案

### 核心控制流程

```python
# 1. 取消可能占用底盘的旧任务
ts = pnc.get_task_state()
if ts.state not in (0, 7, 9):
    pnc.cancel_task(ts.id)
    time.sleep(0.3)

# 2. 构造相对位移请求
req = agibot_gdk.NaviReq()
req.target.position.x = 前后距离（米，正前负后）
req.target.position.y = 左右距离（米，正左负右）
req.target.orientation.z = sin(偏航角/2)
req.target.orientation.w = cos(偏航角/2)

# 3. 发送并等待完成
pnc.relative_move(req)
while True:
    ts = pnc.get_task_state()
    if ts.state in (3, 7, 9):
        time.sleep(0.5)
        break
```

### ChassisController 类功能清单

文件位置：
- 机器人端：`/data/chassis_demo/chassis_controller.py`
- 本地备份：`/home/davie/G2/chassis_controller.py`

| 方法 | 类型 | 说明 |
|------|------|------|
| `move_forward(distance_m)` | 闭环 | 向前移动 N 米 |
| `move_backward(distance_m)` | 闭环 | 向后移动 N 米 |
| `crab_left(distance_m)` | 闭环 | 左蟹行 N 米 |
| `crab_right(distance_m)` | 闭环 | 右蟹行 N 米 |
| `rotate(angle_deg)` | 闭环 | 原地旋转，正逆时针 |
| `move_diagonal(x_m, y_m)` | 闭环 | 斜向移动 |
| `move_sequence(steps)` | 闭环 | 顺序执行多个动作 |
| `velocity_control(vx,vy,vz,duration)` | 开环 | 速度遥控 |
| `stop()` | - | 立即停止 |
| `get_task_state()` | - | 查询当前任务状态 |

---

## 五、任务 state 状态码说明

| state | 含义 |
|-------|------|
| 0 | 空闲（IDLE） |
| 2 | 运行中（RUNNING） |
| 3 | 成功完成 |
| 7 | 已取消（CANCELED） |
| 9 | 完成（SUCCESS，quark_navigation 内部定义） |
| -1 | 等待超时 |

> state=7 和 state=9 均表示动作执行结束，机器人已到位。

---

## 六、关键系统进程一览

| 进程 | 作用 |
|------|------|
| `aorta-service` | DDS 中间件，负责所有进程间通信 |
| `gdk_service` | GDK Python API 的后端服务 |
| `gdk_http_server :8849` | 日志文件 HTTP 服务器（调试用） |
| `quark_navigation` | 导航主进程，管理底盘运动和任务 |
| `genie_motion_control` | 上身关节运动控制（22个关节） |
| `teleop_main_node` | 遥控指令处理节点 |
| `fault_manager` | 硬件故障检测与上报 |
| `slam_state_machine` | SLAM 激光雷达定位状态机 |
| `dr_state_machine` | 死算里程计（IMU，底盘运动必需） |
| `freespace_state_machine` | 自由空间检测 |
| `hal` / `hal_lowerlimb` | 硬件抽象层（上身/底盘） |
| `power_manager` | 电源管理 |

---

## 七、经验总结

1. **优先用 `relative_move`**，不用 `move_chassis`。前者走任务系统，内置 DR 闭环，稳定；后者需要抢控制权，容易被阻塞。

2. **每次运动前先取消占用任务**。`quark_navigation` 同一时间只能有一个活跃任务，不清除会导致新指令被拒绝。

3. **连续动作之间加 0.5s 缓冲**。state=9 报出后，导航系统内部还需时间重置，不等待会触发 `Task is not in IDLE` 错误。

4. **SLAM 不可用不影响底盘运动**。`relative_move` 依赖的是 DR（死算），只需 IMU 工作正常即可。SLAM 只在全局导航（`normal_navi`）时才需要。

5. **调试利器：GDK HTTP 日志服务器**。访问 `http://10.20.15.169:8849/` 可查看所有系统进程的实时日志，排查问题时优先看 `fault_manager.INFO` 和 `quark_navigation.INFO`。
